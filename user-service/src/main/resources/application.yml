server:
  port: 8084
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8088/eureka/
  instance:
    hostname: localhost
spring:
  application:
    name: user-service